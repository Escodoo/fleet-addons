This module adds preventive maintenance alerts for fleet vehicles based on a
distance interval configured per vehicle model. When a new odometer reading is
registered and the distance since the last alert reaches the configured
interval, the module creates an alert record and schedules a mail activity for
the vehicle's fleet manager.
